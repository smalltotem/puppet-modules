  location <%= location %> {
    include <%= fastcgi_params %>;
    fastcgi_pass <%= fastcgi %>;
    <% unless fastcgi_index == :undef %>fastcgi_index <%= fastcgi_index %>;<% end %>
    <% unless fastcgi_script == :undef %>fastcgi_param SCRIPT_FILENAME <%= fastcgi_script %>;<% end %>
  }
