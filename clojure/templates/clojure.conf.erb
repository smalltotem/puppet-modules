#!upstart
description "clojure lein ring server"
author      "OpDemand"

start on (local-filesystems and net-device-up IFACE=eth0)
stop on shutdown

script
    cd /home/<%= scope.lookupvar("clojure::params::username") %>/repo 
    exec lein ring server-headless <%= scope.lookupvar("clojure::params::port") %>
end script
