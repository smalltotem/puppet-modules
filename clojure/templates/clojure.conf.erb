#!upstart
description "clojure lein ring server"
author      "OpDemand"

start on (local-filesystems and net-device-up IFACE=eth0)
stop on shutdown

script
    cd /home/ubuntu/repo
    exec sudo -u <%= scope.lookupvar("clojure::params::username") %> -i lein deps
    exec sudo -u <%= scope.lookupvar("clojure::params::username") %> -i lein ring server-headless 8080
end script
